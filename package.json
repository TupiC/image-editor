{
  "name": "tupi-image-editor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm -r run build",
    "build:core": "pnpm --filter image-editor build",
    "build:sample": "pnpm --filter sample build",
    "watch:core": "pnpm --filter image-editor watch",
    "dev": "pnpm --filter sample dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.6.2"
}
